% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/API_core.R
\name{.ddg.chunk.source}
\alias{.ddg.chunk.source}
\title{.ddg.chunk.source is an alternative to .ddg.source which parses an RMarkdown file, collecting provenance on the code. 
if details are set to TRUE in the chunk header, then detailed provenance is collected on that chunk. Otherwise, just input/output
information is collected.}
\usage{
.ddg.chunk.source(
  file,
  local = FALSE,
  encoding = getOption("encoding"),
  print.eval = getOption("verbose"),
  ignore.ddg.calls = TRUE,
  ...
)
}
\arguments{
\item{file}{is the path of the RMarkdown file to be parsed}

\item{local}{the environment in which to evaluate parsed
expressions. If TRUE, the environment from which .ddg.source is
called. If FALSE, the user's workspace (global environment).}

\item{encoding}{encoding to be assumed when file is a
character string}

\item{print.eval}{print the result of each evaluation}

\item{ignore.ddg.calls}{if TRUE, ignore DDG function calls}
}
\description{
.ddg.chunk.source is an alternative to .ddg.source which parses an RMarkdown file, collecting provenance on the code. 
if details are set to TRUE in the chunk header, then detailed provenance is collected on that chunk. Otherwise, just input/output
information is collected.
}
